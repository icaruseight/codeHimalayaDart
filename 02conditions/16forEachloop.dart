void main() {
  List<String> names = ["Sagar", "Aakash", "Ganga", "Ravi"];
  List<int> numbers = [1, 2, 3, 4, 5, 6];

  names.forEach((name) {
    print(name);
  });
  numbers.forEach((element) {
    print(element);
  });
}
